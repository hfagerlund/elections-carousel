sudo: false
language: node_js
node_js:
  - "7.2.1"
before_install: if [[ `npm -v` != 3* ]]; then npm i -g npm@3; fi
install:
  - npm install
  - npm install -g http-server
before_script:
  - http-server &
script: npm run travis
